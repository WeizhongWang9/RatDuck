//using CardGame.Game.Utilities;
using CardGame.GameEvents.UnitEvents;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
/*
namespace CardGame.Game.GameTerms.Skills
{
    public class CardCast : Cast
    {
        protected CardCast(Unit triggerUnit, ListSelect caster, ListSelect targetSelector) : base(triggerUnit, caster, targetSelector)
        {
        }

        protected override bool beginActiveCondition(LinkedList<Target> targets, out string errorStr)
        {
            errorStr = null;
            return true;
        }
        protected override bool effectActiveCondition(LinkedList<Target> targets, out string errorStr)
        {
            errorStr = null;
            return true;
        }
        protected override bool beginEffect(UnitOrder order)
        {
            return true;
        }
        protected override bool effectEffect(UnitOrder order)
        {
            return true;
        }
        protected override List<Filter> getFilters()
        {
            var list = new List<Filter>();
            return list;
        }
    }


}
*/